<section id="details-info">
    <h1>Details</h1>
    <div class="coin-image">
        <img src="{{crypto.image}}">
    </div>

    <div class="coin-info">
        <div class="coin-text">
            <h1 id="name">{{crypto.name}}</h1>
            <h3 id="payment">Payment method: {{crypto.paymentMethod}}</h3>
            <p id="price"><span>Price: ${{crypto.price}}</span></p>
            <p id="description">{{crypto.description}}</p>
        </div>
        <div class="product-btn">
            <!--Only for registered user and author of the publication -->
            {{#if isUser}}
            {{#if isOwner}}
            <div class="author">
                <a href="/edit/{{crypto._id}}" class="btn-edit">Edit</a>
                <a href="/delete/{{crypto._id}}" class="btn-delete">Delete</a>
            </div>
            {{else}}
            <!-- Logged in user who has not yet buy this crypto-->
            {{#if isBought}}
            <a href="/buy/{{crypto._id}}" class="btn-buy">Buy</a>
            {{else}}
            <p class="buy-message">You already bought these crypto coins.</p>
            {{/if}}
            {{/if}}
            {{/if}}
            <!-- Logged in user who has already buy this crypto-->
        </div>
    </div>
</section>